---
description: Human-readable R analysis scripts — use r-human-code skill
globs: ["**/*.R", "**/*.r", "**/*.Rmd", "**/*.qmd"]
alwaysApply: false
---

# R analysis scripts

Load **`r-human-code`** from this pack when writing or refactoring analysis scripts.

- One script per job where possible; read top to bottom
- `pkg::fun()` for non-base R — no `library()` then bare verbs
- Plain variables at the top for paths and options — no `commandArgs()` / CLI parsing
- No custom functions unless the same chunk repeats and would be verbose inline
- Heavy compute on ANU Dayhoff → separate `.sh` SLURM wrapper (`dayhoff-slurm`)

Do not use generic `r-style-guide`, `tidyverse-patterns`, or `r-tidyverse-style`.
